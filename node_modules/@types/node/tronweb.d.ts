declare module 'tronweb' {
    export class TronWeb {
      constructor(fullNode: string, solidityNode: string, eventServer: string);
  
      // TronWeb 클래스의 메서드 및 속성 타입 선언
    }
  
    export class Contract {
      constructor(tronWeb: TronWeb, abi: any, address: string);
  
      // Contract 클래스의 메서드 및 속성 타입 선언
    }
  
    // 다른 타입 선언 추가
  }
  